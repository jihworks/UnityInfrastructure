# JSON namespace

Some types in this namespace depends on `Newtonsoft.Json`.

## Install Package

Install `com.unity.nuget.newtonsoft-json` as name by Unity Package Manager(UPM).

## Conditional Compile

Add `INFRASTRUCTURE_USE_NEWTONSOFT_JSON` as symbol.
